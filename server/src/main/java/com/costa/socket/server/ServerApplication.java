package com.costa.socket.server;

import com.costa.socket.server.service.ServerRunner;

public class ServerApplication {
    public static void main(String[] args) {
        new ServerRunner().start();
    }
}
